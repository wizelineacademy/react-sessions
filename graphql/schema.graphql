type Query {
  pokemons: [Pokemon]
  pokemon(id: ID!): Pokemon
}

type Pokemon {
  id: ID!
  name: String
  nameWithId: String
  weight: Weight
  image: String
}

type Weight {
  minimum: String
  maximum: String
}
